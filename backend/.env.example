# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (for future use)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=investment_app
DB_USER=postgres
DB_PASSWORD=your_password

# JWT Configuration (for future use)
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=1d
REFRESH_TOKEN_SECRET=your_refresh_token_secret
REFRESH_TOKEN_EXPIRES_IN=7d

# External API Keys (for future use)
COINGECKO_API_KEY=your_coingecko_api_key

# Market Data API Configuration
MARKET_DATA_API_KEY=your_market_data_api_key
MARKET_DATA_PROVIDER=alphavantage  # options: alphavantage, polygon
MARKET_DATA_POLLING_INTERVAL=60000  # in milliseconds (60 seconds)
DEFAULT_ASSET_WATCHLIST=AAPL,MSFT,GOOGL,AMZN,BTC-USD  # comma-separated symbols
